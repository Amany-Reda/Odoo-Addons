<odoo>

    <record id="view_pos_config_form_ration" model="ir.ui.view">
        <field name="name">pos.config.form.ration</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//setting[@id='other_devices']" position="before">
                <setting string="Enable Ration">
                    <field name="enable_ration"/>

                    <div class="content-group" invisible="not enable_ration">

                        <div class="mt16">
                            <label for="un_mission_id"/>
                            <field name="un_mission_id"/>
                        </div>

                        <div class="mt16">
                            <label for="country_id"/>
                            <field name="country_id"/>
                        </div>

                    </div>

                </setting>

            </xpath>

        </field>
    </record>

</odoo>